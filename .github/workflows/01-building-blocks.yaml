name: 01-Building Blocks
on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
          - name: Say hello
            run: echo "hello,World"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
          - name: Failed steps
            run: |
              echo "I will Failed"
              exit 1
          - name: Say goodbye
            run: echo "goodbye"
